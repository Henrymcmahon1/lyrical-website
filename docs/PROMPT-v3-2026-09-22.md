# Paste this to start the v3 session

We are continuing the lyrical two-doors build. All development is on the **`develop`** branch of
`C:\Users\User\CascadeProjects\lyrical-website` (deploys to https://lyrical-studio-omega.vercel.app)
and `main` of `C:\Users\User\CascadeProjects\lyrical-studio` (the pipeline on the render PC).
**Never touch `main` of `lyrical-website` or lyricalglobal.com.**

Read first, in order:
1. `docs/HANDOVER-v3-2026-09-22.md` (scope, my decisions, research, the parallel plan, traps)
2. `docs/superpowers/specs/2026-09-22-two-doors-v2-design.md` (architecture, data model, decision log)
3. `docs/emails/README.md` (the email plan folder to fill)
4. Memory: `lyrical-two-doors-v2`, `lyrical-render-optiplex`, `lyrical-two-sites-boundary`

The v3 scope (decisions already made, do not reopen): sign-in becomes a 6-digit email code plus
Google sign-in (today's failure is the missing Resend key on omega); four automatic emails with a
`docs/emails/` folder of plan, schedule and mock-ups; a fixed turnaround promise per plan in the
studio; a professional `/studio/account` page; a leaner `/artists` with an animated earnings graph
(gross only); a Coffey Anderson before/after slot inside the home page's "no borders" section;
investors page placeholders for deck, flyer and a YouTube-unlisted recording; an `/admin` CRM built
on our Supabase data; Door 2 copyright detection that fingerprints at upload and blocks commercial
matches, with the consequences written into the copy and a takedown policy. Pipeline quality and
Stripe are parked.

Do NOT start building. First: confirm the three numbers I owe you (turnaround promise per plan) and
anything the handover marks as my call, via AskUserQuestion (multiple choice, never a bare list).
Then plan the parallel bots exactly as the handover's section 2 lays out, write one brief per bot
under `docs/bots/v3/`, and run them in worktrees. Review every PR before merging. Keep the handover
and the spec's decision log updated as decisions happen. No em dashes anywhere.
